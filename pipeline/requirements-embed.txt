# Mycelium embed-service (Nomic Embed Text v1.5, ONNX) — pinned deps.
#
# HuggingFace model id: nomic-ai/nomic-embed-text-v1.5
#   files pulled on first use (auto-download, ~170MB int8 ONNX):
#     onnx/model_quantized.onnx
#     tokenizer.json
#
# Install:  python3 -m pip install -r pipeline/requirements-embed.txt
# Run:      python3 pipeline/embed-service.py --serve
# CPU-only; no GPU / CUDA packages. Python >= 3.10. (requirements.txt is the
# separate, heavier topology-pipeline dependency set — not this one.)

numpy==2.1.3
onnxruntime==1.20.1
tokenizers==0.21.0
huggingface-hub==0.27.0
